name: CI

on:
  schedule:
    - cron: "00 00 * * 4" # every friday at 00:00
  pull_request:
    branches: ["main"]
    paths:
      - "lua/**"
      - "tests/**"
      - "scripts/**"
      - "*.lua"
      - "Makefile"
      - ".github/workflows/test.yml"
  workflow_call:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        nvim-version: [stable, v0.11.0, nightly]
    steps:
      - name: checkout
        uses: actions/checkout@v6

      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: ${{ matrix.nvim-version }}

      - name: Make install
        run: make install
        timeout-minutes: 1

      - name: Run tests
        run: make test
        timeout-minutes: 1
